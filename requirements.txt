# Audio processing
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0

# Chatterbox dependencies (NO TORCH, NO TRANSFORMERS VERSION LOCK!)
omegaconf
safetensors
huggingface-hub
s3tokenizer
conformer
diffusers
einops
gradio

# Note: Uses whatever transformers version is already installed
# Note: Assumes PyTorch XPU is already installed

