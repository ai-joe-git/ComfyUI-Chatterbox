# Audio processing
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0

# Chatterbox dependencies
omegaconf
safetensors
huggingface-hub
s3tokenizer
conformer
diffusers
einops
gradio

# ⚠️ IMPORTANT: Perth watermarking (required by chatterbox)
resemble-perth

# Note: Does NOT include torch/torchaudio (uses ComfyUI's existing PyTorch)
# Note: chatterbox-tts installed separately with --no-deps (see install.bat)

