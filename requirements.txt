# Audio processing (safe - no torch dependency)
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0

# Chatterbox dependencies ONLY (manually listed, NO TORCH)
omegaconf
safetensors
huggingface-hub
s3tokenizer
conformer
diffusers
einops
gradio

# NOTE: chatterbox-tts is NOT in requirements.txt!
# Install it separately with --no-deps (see install.bat)
